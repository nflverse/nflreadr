# Show dependency versions of installed packages

Show dependency versions of installed packages

## Usage

``` r
.sitrep_pkg_deps(
  packages,
  inst_pkgs = utils::installed.packages(),
  recursive = TRUE
)
```
